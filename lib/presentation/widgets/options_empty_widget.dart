import 'package:flutter/material.dart';

class OptionsEmptyWidget extends StatelessWidget {
  const OptionsEmptyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("List is empty"),
    );
  }
}
